name: đź›ˇď¸Ź StraĹĽnik Kodu â€” kontrola regresji

# Uruchamia siÄ™ przy KAĹ»DYM pushu i pull requeĹ›cie do main â€” niezaleĹĽnie od
# tego, ktĂłry agent (Claude, Antigravity, Codex) czy czĹ‚owiek wprowadza zmianÄ™.
# Cel: ĹĽadna z klas bĹ‚Ä™dĂłw opisanych w STRAZNIK_JAKOSCI_KODU.md nie wraca
# niezauwaĹĽona.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: straznik-kodu-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  straznik-kodu:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Pobierz kod
        uses: actions/checkout@v5

      - name: Ustaw Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Zainstaluj zależności
        run: npm ci

      - name: Uruchom kontrole regresji LUMINA
        run: npm test

      - name: Wynik
        if: failure()
        run: |
          echo "::error::StraĹĽnik Kodu znalazĹ‚ naruszenia znanych klas regresji. Zobacz log powyĹĽej â€” napraw przed mergem."
